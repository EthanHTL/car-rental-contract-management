<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.carrentalcontract.mapper.RoleMapper">

    <select id="findRoleByUserName" resultType="com.example.carrentalcontract.entity.model.SysRole">
        select sr.*
        from sys_role sr
                 left join sys_user_role sur on sr.id = sur.role_id
                 left outer join sys_users su on sur.user_id = su.id
        where su.username = #{username}
    </select>
</mapper>